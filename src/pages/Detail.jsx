import Back from '../components/Back';

const Detail = (props) => {
  console.log(props);

  return (
    <section>
      <div className='container'>
        <Back />
      </div>
    </section>
  );
};

export default Detail;
